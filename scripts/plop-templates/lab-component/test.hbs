import { renderComponent } from '@superdispatch/ui-testutils';
import { {{pascalCase name}} } from '../{{pascalCase name}}';
import { screen } from '@testing-library/react';

test('{{pascalCase name}} renders', () => {
  renderComponent(<{{pascalCase name}}>{{pascalCase name}}</{{pascalCase name}}>);
  expect(screen.getByTestId('{{camelCase name}}')).toMatchSnapshot();
});
