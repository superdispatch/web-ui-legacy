import React from 'react';
import { Meta } from '@storybook/react';
import { {{pascalCase name}} } from './{{pascalCase name}}';

export default { title: 'Lab/{{pascalCase name}}', component: {{pascalCase name}} } as Meta;

export const basic = () => (
  <{{pascalCase name}}>
    {/* Some component props and children here */}
  </{{pascalCase name}}>
);
